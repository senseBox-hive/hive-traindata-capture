## IDF Component Manager Manifest File
dependencies:
  espressif/esp32_p4_function_ev_board_noglib:
    version: ^4.0.1
    rules:
    - if: target == esp32p4
  espressif/esp32_s3_eye_noglib:
    version: ^3.1.0~1
    rules:
    - if: target == esp32s3
  espressif/esp32-camera: ^2.0.15
  espressif/esp-dl: "*"
  espressif/esp_new_jpeg: ^0.6.1
  espressif/esp_image_effects: ^1.0.1
